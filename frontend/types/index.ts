export type ModelProps = {
  name: string;
  numParam: string;
  parentID?: string;
};

export type ModelNFTProps = {
  creator: string;
  createdAt: string;
  numParams: string;
  modelName: string;
};
